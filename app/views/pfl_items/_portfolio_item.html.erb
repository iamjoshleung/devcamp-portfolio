<p><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item) %></p>
<p><%= portfolio_item.subtitle %></p>
<p><%= portfolio_item.body %></p>
<%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil? %></p>
<p><%= link_to "Edit", edit_pfl_item_path(portfolio_item) %></p>
<p><%= link_to 'Delete Portfolio Item', pfl_item_path(portfolio_item), method: :delete, data: { confirm: 'Are you sure?' } %></p>
<hr>